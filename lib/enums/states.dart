enum AppState { idle, busy }
enum BackgroundType { main, content }
enum TransactionType { credit, debit }
enum AdminMenu { edit, block }
enum HomeMenu { logout, about }
enum TransactionCategory { airtime, data, cables, power, giftcard, crypto, withdrawal }